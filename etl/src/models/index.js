const { BusinessAccount } = require('./business-account.model');
const { MetaCampaign, MetaAdSet, MetaAd, MetaPerformance } = require('./meta-ads.model');

module.exports = {
  BusinessAccount,
  MetaCampaign,
  MetaAdSet,
  MetaAd,
  MetaPerformance
};
